// Задача 1: Создание первой переменной (Легкая) - РЕШЕНИЕ

void main() {
  // 1. Создаем переменную name с именем
  var name = 'Иван';

  // 2. Создаем переменную age с возрастом
  var age = 20;

  // 3. Выводим сообщение с использованием переменных
  // Используем интерполяцию строк с помощью $
  print('Меня зовут $name, мне $age лет');

  // Альтернативный способ - конкатенация строк
  // print('Меня зовут ' + name + ', мне ' + age.toString() + ' лет');
}
